export interface Graph1 {
  device_id: string;
  points: number;
  date: string;
  displayName: string;
}
export interface Graph2 {
  device_id: string;
  points: number;
  date: string;
}

export interface table {
  id: number;
  device_id: string;
  points: number;
  date: string;
  displayName: string;
}
